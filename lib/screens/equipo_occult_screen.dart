import 'package:flutter/material.dart';

class OccultScreen extends StatelessWidget {
   
  const OccultScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('OccultScreen'),
      ),
    );
  }
}