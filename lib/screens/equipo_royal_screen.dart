import 'package:flutter/material.dart';

class RoyalScreen extends StatelessWidget {
   
  const RoyalScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('RoyalScreen'),
      ),
    );
  }
}