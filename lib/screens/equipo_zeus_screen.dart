import 'package:flutter/material.dart';

class ZeusScreen extends StatelessWidget {
   
  const ZeusScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('ZeusScreen'),
      ),
    );
  }
}